{
  "name": "hamropay-sample-store",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A zero-dependency Node.js T-shirt store demonstrating Hamro Pay Checkout.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "setup": "node scripts/setup.mjs",
    "test": "node --test",
    "check": "for f in src/*.mjs src/hamropay/*.mjs src/services/*.mjs src/web/*.mjs src/web/routes/*.mjs scripts/*.mjs public/*.js; do node --check \"$f\" || exit 1; done && echo OK",
    "build": "npm run check",
    "preflight": "node scripts/preflight.mjs"
  }
}
